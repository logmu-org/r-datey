# Generic operators for `datey` and `durationy`

Generic operators for `datey` and `durationy`

## Usage

``` r
# S3 method for class 'datey_type'
Ops(e1, e2)
```

## Arguments

- e1:

  First (`datey`) parameter.

- e2:

  Second parameter – must be `datey` or `durationy`.
